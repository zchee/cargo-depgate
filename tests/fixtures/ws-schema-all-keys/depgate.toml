schema = 1

[graph]
features = "all"

[internal]
members = false
patterns = ["workspace-*"]

[manifest]
versions-in-root = false

[rules."workspace-app"]
deny = ["openssl", "ring*"]
require = ["serde"]
internal = ["workspace-core"]
direct = ["serde"]
sealed = true

[rules."workspace-core"]
leaf = true
